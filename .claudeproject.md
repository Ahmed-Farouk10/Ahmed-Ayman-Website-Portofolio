# Claude Project Manifest: Masterpiece Renovation Directive
Mandate: Rebuild Ahmed's Portfolio into an Awwwards-League Visual Masterpiece

Welcome, Claude. You are acting as the **Lead Shaders & Interaction Engineer**. Your supreme mandate is to execute the masterpiece portfolio overhaul as detailed in `docs/PLAN-masterpiece-renovation.md`. We are discarding the standard "Tailwind template" layouts and building an out-of-this-world, elite creative portfolio.

---

## 🚫 Critical Directives & Boundaries
- **DO NOT TOUCH** Sanity schemas or `/studio`. Keep backend data-fetchingGROQ queries completely intact.
- **Visual Stature:** Strict matte carbon theme `#030305`, raw mercury white typography, microscopic neon cyan accents, zero cheap gradients, zero lag.
- **Target Performance:** Maintain locked 60 FPS. Optimize canvas sizes and Matter.js engine loop ticks, pausing the physics updates when the section is out of the viewport.

---

## 🗺️ Masterpiece Implementation Phases

### Phase 1: Installation & Design System Setup
- Install dependencies inside `web/`:
  - `matter-js` (physics engine)
  - `split-type` (typographic splitting)
- Update `globals.css` to define the carbon theme system, custom layout parameters, and editorial fonts.

### Phase 2: R3F Molten Liquid Glass Shader Canvas (`HeroCanvas.jsx`)
- Refactor the canvas to render a single custom Mesh.
- Write custom Vertex and Fragment shaders in R3F computing a Simplex noise displacement field that ripples elastically based on relative mouse coordinates, resembling molten glass or flowing mercury.

### Phase 3: Matter.js Skills Physics Playground (`Skills.js`)
- Refactor the Skills block to build a Matter.js 2D physics viewport:
  - Spawn floating physical bodies with custom bounds representing skills text tags.
  - Set mouse constraints permitting grab-and-toss inertia.
  - Implement canvas scaling on viewport changes.

### Phase 4: Horizontal Projects Slider with Pinned GSAP ScrollTriggers (`Projects.js`)
- Refactor `Projects.js` to trigger a GSAP pinning event on vertical scroll.
- Slide projects horizontally from the right to the left with custom opacity sweeps.
- Wrap project titles with `SplitType` staggers, revealing letters one-by-one from behind clip-path masks.

---

## 🧠 Memory Tracking Protocol
- Read `docs/PLAN-masterpiece-renovation.md` immediately to understand the visual logic.
- Update `.claudehistory.md` at the end of every successful execution phase to preserve project progress variables across terminal sessions.
